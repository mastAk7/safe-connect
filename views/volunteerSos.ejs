<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Volunteer SOS</title>
  <link rel="stylesheet" href="css/volunteer-sos.css">
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css" />
  <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
</head>
<body>
  <style>
    * {
  box-sizing: border-box;
  font-family: 'Segoe UI', sans-serif;
}

html, body {
  margin: 0;
  padding: 0;
  height: 100%;
  background: #f0f4ff;
}

.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

header {
  background-color: #4e54c8;
  color: white;
  padding: 20px;
  text-align: center;
  font-size: 24px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

main {
  flex: 1;
  overflow-y: auto;
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 30px;
}

.victim-list {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.victim-card {
  background: white;
  padding: 15px 20px;
  border-radius: 10px;
  border-left: 6px solid #4e54c8;
  cursor: pointer;
  transition: background 0.2s;
}

.victim-card:hover {
  background: #f3f6ff;
}

.victim-card h3 {
  margin: 0 0 5px;
  font-size: 18px;
}

.victim-card p {
  margin: 0;
  color: #555;
  font-size: 14px;
}

.emergency-section {
  background: #fff;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 3px 10px rgba(0,0,0,0.05);
}

.emergency-section h2 {
  margin-top: 0;
  color: #b30000;
  font-size: 22px;
}

.highlight {
  border-left-color: #ff4d4d;
}

.actions {
  margin-top: 10px;
  display: flex;
  gap: 10px;
}

.chat-btn, .call-btn {
  flex: 1;
  padding: 10px;
  border: none;
  border-radius: 8px;
  font-weight: bold;
  cursor: pointer;
  font-size: 14px;
}

.chat-btn {
  background: #cde6ff;
  color: #005bbb;
}

.call-btn {
  background: #ffd2d2;
  color: #a30000;
}

  </style>
  <div class="container">

    <main>
      <h1>Alerts</h1>
      <% if (emergencyAlerts && emergencyAlerts.length > 0) { %>
        <section class="victim-list">
        <% emergencyAlerts.forEach(alert => { %>
              <div class="victim-card" onclick="openVictimProfile('Anita Sharma')">
                <h3><%= alert.user.name %></h3>
                <p><%= alert.addressDisplay %></p>
                <p><%= alert.distance %> km away</p>
                <p> Emergency type : <%= alert.emergencyType %></p>
                <div class="actions">
                  <button class="chat-btn">Chat</button>
                  <button class="call-btn"> Call</button>
                  <button class="chat-btn">Connect</button>
                </div>
              </div>
        <% }); %>
        </section>
      <% } else { %>
        <p class="no-alerts">No emergency alerts at this time.</p>
      <% } %>
      
    </main>
  </div>

  <script>
    function openVictimProfile(name) {
      alert(`Opening profile for ${name}...\n\n(View victim details & incident info)`);
    }
  </script>
</body>
</html>
